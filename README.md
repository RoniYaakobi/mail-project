# How to run project?
To run this project you must run the run_server.bat file, and then run as many clients as you want, by running run_client.bat

If you arent on windows, you can copy the first line of each file to run the code regardless of the OS.
